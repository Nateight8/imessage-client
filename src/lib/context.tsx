// import React, { ReactNode, createContext } from "react";
// import { ParticipantPopulated } from "../../../imessage/apollo-server/src/schema/types.utils";

// type contextProps = {
//   participants: ParticipantPopulated[];
// };

// export const ChatContext = createContext({
//   participants: [],
// });

// interface Props {
//   children: ReactNode;
// }

// export const ChatContextProvidier = ({ children }: Props) => {
//   return <ChatContext.Provider value={{}}>{children}</ChatContext.Provider>;
// };

// interface Props {
//   children: ReactNode;
// }
